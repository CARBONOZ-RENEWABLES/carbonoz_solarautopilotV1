<!DOCTYPE html>
<html>
<head>
    <title>Grafana Dashboard</title>
    <style>
        body { margin: 0; padding: 0; }
        iframe { width: 100%; height: 100vh; border: none; }
        .header { 
            background: #333; 
            color: white; 
            padding: 10px; 
            text-align: center; 
            font-family: Arial, sans-serif;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>Grafana Dashboard</h2>
        <small>Path: <%= basePath || 'Direct' %></small>
    </div>
    <iframe src="<%= dashboardUrl %>" title="Grafana"></iframe>
    
    <script>
        console.log('Grafana Base URL:', '<%= grafanaUrl %>');
        console.log('Dashboard URL:', '<%= dashboardUrl %>');
        console.log('Base Path:', '<%= basePath || "Direct" %>');
    </script>
</body>
</html>